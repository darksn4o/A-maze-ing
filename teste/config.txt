# A-Maze-ing default configuration.
# One KEY=VALUE pair per line. Lines starting with '#' are ignored.

# --- Mandatory keys ---
WIDTH=20            # maze width  (number of cells, x axis)
HEIGHT=15           # maze height (number of cells, y axis)
ENTRY=0,0           # entry coordinates (x,y)
EXIT=19,14          # exit coordinates  (x,y)
OUTPUT_FILE=maze.txt
PERFECT=True        # True -> exactly one path between entry and exit

# --- Optional keys ---
SEED = 42             # remove for a different maze on every run
PATTERN=True        # draw the centred "42" made of fully closed cells
ANIMATE=True        # animate the carving on screen (ignored when piped)
ANIMATION_SPEED=0.25  # 1.0 = normal; below 1 is slower, above 1 is faster
